apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-product
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/luc1fe4/yas.git'
    targetRevision: 'fix-grafana'
    path: k8s/charts/product
    helm:
      parameters:
        - name: backend.image.tag
          value: 'v1.0.0'
        - name: backend.image.repository
          value: 'besukem/product'
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
